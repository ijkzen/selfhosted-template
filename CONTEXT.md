# selfhosted-template

通用单用户自托管 Web 应用模板（Rust + React 单体），从 llm-gateway 抽离全部业务逻辑、保留前后端基础设施而来。当前域词汇以模板的通用能力为主，随会话逐步补充。

## Language

### 认证域

**用户 (User)**:
单用户后台的登录身份，用户名 + argon2id 密码哈希。
_Avoid_: 账号、管理员、成员

**会话 (Session)**:
服务端 cookie 会话；令牌以 SHA-256 哈希为主键存储，固定过期时间，可注销/踢旧会话。
_Avoid_: token（裸称）、登录态

**初始化 (Initialization)**:
首次启动时的建号流程：系统未初始化时拒绝登录，仅开放 init 接口创建首个用户并写入默认设置。
_Avoid_: 注册、首跑引导

### 设置域

**设置 (Setting)**:
键值对配置项，带类型枚举（字符串/整数/布尔/JSON）；语言与时区是模板自带的两项种子设置。
_Avoid_: 配置项（裸称）、环境变量（不同概念）

### 定时任务域

**定时任务 (CronJob)**:
可管理的定时任务配置：cron 表达式（含 `@every` 间隔）、时区语义与启停开关；与具体业务 handler 解耦注册。
_Avoid_: 任务（裸称）、job（裸称）

**任务执行 (CronJobRun)**:
一次任务的单次执行记录：触发时间、耗时与执行状态。
_Avoid_: 运行记录（裸称）

**任务日志 (CronJobLog)**:
按执行组织的日志流；实时经 SSE 推送，历史持久化并定期清理。
_Avoid_: 输出（裸称）、log（裸称）

### 示例域

**笔记 (Note)**:
模板自带的示例资源，演示"列表 + 新建 + 编辑 + 删除"的完整前后端 CRUD 骨架；新项目应以它为母版替换成自己的业务域。
_Avoid_: 示例（裸称）、todo、条目

### 通用约定

**统一响应 (ApiResponse)**:
前后端约定的响应信封 `{code, msg, data}`；`code = "0"` 表示成功，非零为错误码。前端对非零 code 统一抛错。
_Avoid_: 裸 JSON、HTTP 状态码直传（不同概念）